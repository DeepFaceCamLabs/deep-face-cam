# Minimal dependency set for the bundled macOS backend sidecar.
#
# Keep this smaller than backend/requirements.txt: the packaged Tauri app does
# not use the legacy PySide UI, TensorFlow NSFW classifier, or non-macOS GPU
# runtime packages.
numpy>=1.23.5,<2
typing-extensions>=4.8.0
opencv-python==4.10.0.84
cv2_enumerate_cameras==1.1.15
onnx==1.21.0
insightface==0.7.3
psutil==5.9.8
pillow==12.1.1
tqdm>=4.65.0
onnxruntime-silicon==1.16.3; platform_machine == 'arm64'
onnxruntime==1.23.2; platform_machine != 'arm64'
protobuf==4.25.1
aiohttp>=3.9
PyInstaller>=6.11,<7
